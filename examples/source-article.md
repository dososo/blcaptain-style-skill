# Claude Code Sub-Agents 实战：如何让 5 个调研任务同时跑

很多人以为并行调研就是多开几个窗口。其实不是。

真正能提速的，是把一个大问题拆成几个互不阻塞的小问题。每个 Sub-Agent 只拿自己需要的上下文，只负责一个判断。

这件事最容易翻车的地方，是总编缺位。没有总编，5 个 Agent 只是在制造 5 份噪声。

一个稳定流程应该是：先拆任务，再分角色，然后并行检索，最后统一证据、去重、排序，形成结论。

我的经验是，Sub-Agent 不要追求“聪明”，要追求“边界清楚”。边界清楚，输出才可合并。
